<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sun Bee — Ваш проводник в мире</title>
    <style>
        /* Общие стили для всей страницы */
        body {
            font-family: Arial, sans-serif; /* Устанавливаем шрифт */
            text-align: center; /* Выравниваем текст по центру */
            background: linear-gradient(45deg, #ffcc00, #ff6600); /* Градиентный фон */
            color: white; /* Белый текст */
        }

        /* Заголовок */
        h1 {
            font-size: 36px;
            margin-top: 50px;
        }

        /* Обычный текст */
        p {
            font-size: 20px;
        }

        /* Кнопка */
        .btn {
            display: inline-block; /* Кнопка ведет себя как блочный элемент */
            padding: 15px 25px; /* Внутренние отступы */
            font-size: 20px; /* Размер текста */
            color: white; /* Белый текст */
            background-color: #0088cc; /* Цвет кнопки */
            text-decoration: none; /* Убираем подчеркивание */
            border-radius: 10px; /* Закругляем углы */
            transition: 0.3s; /* Плавное изменение цвета */
        }

        /* Эффект наведения на кнопку */
        .btn:hover {
            background-color: #005f99; /* Меняем цвет */
        }

        /* Контейнер для кнопки */
        .container {
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <!-- Заголовок страницы -->
    <h1>Sun Bee 🐝</h1>
    <p>Ваш проводник в мире</p>

    <!-- Кнопка для перехода -->
    <div class="container">
        <a href="#" class="btn">Перейти в раздел новостей</a>
    </div>
</body>
</html>
